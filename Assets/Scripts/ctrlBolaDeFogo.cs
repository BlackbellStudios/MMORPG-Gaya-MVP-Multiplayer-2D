using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ctrlBolaDeFogo : MonoBehaviour
{
	/*
	float fVelocidade = 10;
    Vector2 vAlvo;
    string sFrind;
    Collider2D meuColisor;
    // Use this for initialization
    void Start()
    {
      
    }

    // Update is called once per frame
    void Update()
    {


        if (vAlvo != null)
        {

            transform.position = Vector3.MoveTowards(transform.position, vAlvo, 0.1f);
            if (transform.position.x== vAlvo.x && transform.position.y == vAlvo.y)
            {

                Destroy(transform.gameObject);
            }
        } 

    }
    public void SetarAlvo(string salvo)
    {
        sFrind = salvo.Split('|')[2];
        vAlvo = new Vector2(float.Parse(salvo.Split('|')[0]), float.Parse(salvo.Split('|')[1]));
        
        //print

    }

    void OnTriggerEnter2D(Collider2D other)
    {
        print(other.name);
        if(other.gameObject.tag != "ObjectScene"){
        Destroy(other.gameObject);
        Destroy(this.gameObject);
        }
        
    }
    */
}
